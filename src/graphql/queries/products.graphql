query Products {
    productCollection(limit: 10) {
        items {
            productName
            imageUrl
            price
            salePrice
            slug
            sys {
                id
            }
          }

    }
}